#include "nothing.h"
#include "test.h"


void main ( void )
{
    int cntr = nothing;
    while( 1 )
    {
        output( "stuff here too" );
        cntr -= testing;
        if( cntr == 0 )
            cntr = nothing;

        nop();

        if( cntr == 1.5 )
            break;
    }
}
void main ( void )
{
    int cntr = nothing;
    while( 1 )
    {
        output( "stuff here too" );
        cntr -= testing;
        if( cntr == 0 )
            cntr = nothing;

        nop();

        if( cntr == 1.5 )
            break;
    }
}
